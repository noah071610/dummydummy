import styled from '@emotion/styled';
import tw from 'twin.macro';

export const AsideWrapper = styled.aside`
  ${tw`w-full border-r-1px border-solid border-border`}/* box-shadow: 5px 0 10px rgba(0,0,0,0.15); */
`;
