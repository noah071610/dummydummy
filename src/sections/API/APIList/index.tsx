import { APISectionWrapper } from './styles';

interface IProps {}

function APISection({}: IProps) {
  return (
    <APISectionWrapper>
      <h1>트리거 API 목록</h1>
    </APISectionWrapper>
  );
}

export default APISection;
